尺规作图 · 更新日志

[2.5]-2026-09-12 · 中英文切换

线上地址不变：https://compassgame2-5.netlify.app/

新增
- **中英文界面切换**：右侧新增「中/EN」按钮，一键切换整套界面语言——按钮、工具栏、HUD、过关面板、选关界面、23 个关卡的名称与目标全部人工翻译（非机翻），英文模式下界面瞬间全英文
- **语言记忆**：语言选择自动保存（网页存 localStorage，电脑存 lang.dat），下次打开保持上次的选择

工程化（维护性，玩家无感）
- 新增 `lang.py` 语言包：中文作为钥匙查表翻译，关卡名/目标按序号查表，以后加新关卡只需在两个列表里各加一行英文

[2.4]-2026-09-06 · 选关界面 🗂️

新增
- **选关界面**：右侧新增「选关」按钮，展开 5×5 关卡格子面板——每格显示关卡号、名称、最好成绩（S/A/B/C/D 金色标记），点格子直达目标关卡，再也不用狂点 23 次「下一关」
- **进度锁定**：未通关的关卡灰色锁定（第 N 关解锁条件 = 第 N-1 关有成绩），当前关卡金色描边高亮；ESC 或点空白处关闭面板

修复
- **读档丢工具**：读档代码里 `else` 错配给了错误的 `if`，导致读档恢复到第 11~18 关时角平分线工具被误没收；现统一为 `refresh_tool_unlocks()` 按关卡号恢复，读档、选关共用同一套标准
- **工具解锁标准统一**：过关解锁（`==18`）与读档恢复（`>=19`）原先差一关不一致，现已统一

[2.3]-2026-08-29 · 关卡扩容 + 界面焕新 📐

新增（关卡）
- **11 个新关卡**（12~19，含变体共 23 关）：
  做 30° 角、做 2 倍角、过直线外一点分割矩形为两等积部分、
  过直线外一点作垂线、过直线上一点作垂线、
  作与圆切于某点的直线、作与已知直线相切的圆 等
- **垂线工具**：通关第 19 关后解锁，指定点 + 指定线一键作垂线
- **关卡进度显示**：HUD 实时显示「第 x 关 / 共 23 关」，再也不用猜打到哪了

新增（界面）
- **坐标纸风格 UI**：网格纸背景 + 全新排版的信息面板，告别简陋白底
- 按钮区布局整理，撤销、存读档、切关按钮各就各位

修复（判定系统）
- **蒙混过关封堵**：修复多关「随便圈个范围甚至什么都不点就判过」的漏洞——现在所有关卡在空初始状态下绝不可能通过；垂直平分线等关卡要求真实作出目标线，不再只查「存在这条线」
- **切线判定补全**：点积漏算 y 分量导致方向判断出错，现已修正并加入长度方向校验
- **相切圆判定修正**：给定点的引用、点到直线距离、圆心方向判断逐一修正
- **撤销后过关状态显示**：撤销操作后过关标记残留的问题已修复
- **工具解锁时机**：各工具解锁关卡数修正（原先的差一错误导致解锁晚一关）

修复（工具）
- **角平分线支持平角**：对 180° 平角使用角平分线工具时返回其垂线——「过直线上一点作垂线」的经典尺规做法（平角平分）现在行得通了
- 修复垂线工具预览时 `perpendicular_through_point()` 参数缺失导致的崩溃

提速（网页版）
- **字体子集全自动化**：打包流水线自动从源码收集用到的汉字生成字体子集（38 KB），新加关卡文字再也不会缺字显示方框；游戏包从约 15 MB 瘦身至 45 KB，手机加载大幅提速

工程化（维护性，玩家无感）
- 新增《关卡开发手册》：给定元素坐标、判定函数写法、函数命名一览，照着填就能写新关卡
- 判定函数命名规范化，每个关卡函数名见名知意

[2.2]-2026-08-28 · 加载提速 + 代码大扫除 🚀

提速（网页版）
- **运行时自托管**：Python 解释器、pygame 引擎等 22 MB 运行时文件不再从 GitHub CDN 现拉，全部随网站一起部署在 Netlify。首次加载从 40 分钟以上降至 2 分钟以内，二次访问走浏览器缓存近乎秒开
- **字体子集化**：中文字体从完整微软雅黑（约 15 MB）精简为只含游戏用字的 110 KB 子集

新增
- **撤销按钮**：屏幕按钮区新增「撤销」，手机党不用再找 Ctrl+Z
- **吸附高亮提示**：靠近可吸附点时给出视觉反馈，瞄准更直观

修复
- **给定圆圆心泄漏**：给定圆（非玩家所画）的圆心会被误当作可用作图点，现已隔离
- **隐藏交点判定补全**：所有添加点的路径统一经过「本关是否隐藏辅助交点」判定，杜绝漏网之鱼
- 吸附判定半径调整为 15px，配合高亮降低「差一点点就判不过」的挫败感

重构（维护性，玩家无感）
- 删除重复/废弃源码文件，绘制函数统一收编至 ui.py
- 四个工具分支中各自为战的交点计算统一为 `recalculate_intersections()`，新增工具只需写画法，不用抄判定
- 变量命名与函数归位整理，告别「聪明人的变量」

工程化
- 新增一键打包流水线（`打包网页/`）：双击 bat 自动完成 pygbag 打包、运行时组装、路径补丁、18 项自检；部署仍只需把 `build/web` 拖进 Netlify

[2.1]-2026-08-27 · 网页版上线 🎉

首个正式可玩版本，已部署至：https://compassgame2-1.netlify.app/

新增
- 网页版支持（pygbag 打包）：字体、存档（localStorage）、异步主循环全平台适配
- 部署至 Netlify，浏览器直接游玩，无需安装

修复（判定系统）
- **第 9 关 内接正方形**：原先任意 4 条圆上弦即可过关；现严格验证「4 点构成正方形（4 边 ≈ r√2、2 对角线 ≈ 2r）且 4 边真实画出」，同时支持作图辅助圆留下的额外交点（组合判定，不再误杀正确解法）
- **第 11 关 角平分线**：原先要求线的端点贴在顶点 5px 内，导致工具画出的线（端点在 2000px 外）永远无法过关；改为「顶点到直线距离 + 共线」判定
- **第 12 关 三角形内接菱形**：原先只要求「顶点出发两条线等长」，搭给定边的便车即可过关；现验证完整菱形（等长邻边 + 第四顶点落对边 + 边线齐全），并新增 `line_covers` 判定，允许菱形的边直接复用三角形给定边
- **第 4 关 垂直平分线**：垂直判定容差未归一化（短线可蒙混）；改为 cosθ < 0.05 归一化判定

修复（稳定性）
- **存档**：修复新成绩无条件覆盖历史最好成绩的问题；修复同评级再次保存时字段名笔误（`step`/`steps`）导致的 KeyError 闪退
- **几何内核**：修复两圆近相切时 `sqrt` 负数导致的 math domain error 崩溃
- **角平分线工具**：修复预览块缺少 `mouse` 赋值导致的 UnboundLocalError 闪退；修复三次点击全程无视觉反馈（红色预览线 + 蓝色标记点现已正常）

修复（逻辑）
- 角平分线工具解锁时机修正：通过第 11 关后解锁（原先需打穿最后一关，解锁即毕业）
- 关卡结构调整：移除重复的「做内接正方形 9.2」，全游戏共 12 关

[2.0 及更早]-2026-08-14~2026-08-23

历史版本按文件夹归档（`compass_game1.0` ~ `compass_game2.0`），主要演进：

- **1.0**：基础玩法——点/线/圆工具，关卡 + 过关判定
- **1.1**：撤销（Ctrl+Z）、关卡切换
- **1.2**：不再显示辅助交点（纯靠吸附），难度提升
- **1.3**：评分系统（步数/元素数/用时 → S/A/B/C/D 评级）
- **1.4**：垂直平分线工具 + 工具解锁机制
- **1.5**：存档/读档（base64 + MD5 校验）、运行日志
- **2.0**：角平分线工具、屏幕按钮（存档/读档/上下关/重开）、网页版适配开始
- **2.1**：底部工具栏取代数字键切换
